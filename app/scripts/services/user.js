'use strict';

/**
 * @ngdoc service
 * @name quetesobraApp.user
 * @description
 * # user
 * Service in the quetesobraApp.
 */
angular.module('quetesobraApp')
  .service('user', function user() {
    // AngularJS will instantiate a singleton by calling "new" on this function
  });
